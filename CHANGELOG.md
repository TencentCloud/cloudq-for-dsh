# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] - 2026-09-02

### 调整

- 面板 API 调用全面迁移到 Node 原生实现（新增 `tcloud.js`，进程内完成 TC3-HMAC-SHA256 签名）：凭证测试/保存/登出/状态、用量、灵感、制品、架构图不再依赖 Python 环境，Windows/Linux/macOS 开箱即用。
- 凭证文件与 CloudQ Skill 保持同路径、同格式、同权限（600），双向兼容。
- 移除已无引用的 Python 子进程桥（`script-runner`）；安全契约升级为「不存在任何携带凭证的子进程」。

## [0.2.1] - 2026-09-02

### 新增

- 版本检测与一键更新：插件加载时自动比对 npm 最新版本，有过期版本时侧栏 CloudQ 按钮出现感叹号徽标，hover 显示版本对比，点击自动更新并重启 DSH 服务（Windows 回退为手动提示）。

### 修复

- 修复 Windows 上点「测试连接」报 502：Python 命令按 `python3 → python → py` 探测（0.3.0 起面板链路已完全不依赖 Python）。
- 宿主缺 Python 环境时显示真实原因，不再误报「AKSK 无效」。

## [0.2.0] - 2026-09-02

### 新增

- CloudQ 会话回复完成后，制品列表后台静默刷新（无 loading），新产出自动出现；折叠的会话组在刷新后保持折叠。

### 调整

- 制品下载改为本地直接保存，不再闪现一次性标签页。
- 架构图卡片点击跳转真实控制台架构图页（`advisor?archId=xxx`），不再打开 SVG 原图。
- 用量/灵感/制品/架构图四个页签去除数据缓存，每次切换实时拉取最新数据。
- 面板展开图标下移到会话头部分隔线下方，不再与「Session log」重叠。

### 修复

- 修复 `create_role.py` 角色名记录错误与配置保存丢字段问题（Skill 资产）。

## [0.1.9] - 2026-09-02

### 修复

- 修复 CloudQ 会话标签不准确：同名会话不再互相影响（按真实会话 ID 匹配），发送 `/cloudq` 消息时立即打标，宿主日志证据改为只增不删的兜底回填，未配置的会话不再因扫描时序丢失标签。

## [0.1.8] - 2026-09-01

### 调整

- CloudQ 会话请求的来源字段（`Source`）固定上报为 `dsh`，不再由调用方传入。
- Skill 注册的 `source` 恢复为 `bundled`（0.1.7 的标记改动回退，会话来源以上述请求字段为准）。

## [0.1.7] - 2026-09-01

### 调整

- 内置 `cloudq` Skill 的 `source` 标记为 `dsh`。

## [0.1.6] - 2026-08-31

### 文档

- README 增加「使用步骤」说明（设置 → 插件 → 配置 AK/SK → 进入 CloudQ 模式），内容同 0.1.5 功能（深色模式适配）。

## [0.1.5] - 2026-08-31

### 新增

- 适配 DSH 深色模式：跟随 `body[data-ds-dark-theme]` 切换整套界面配色（对话首页推荐问题、能力面板、设置卡片、插件管理与架构图预览），浅色模式视觉保持不变。

## [0.1.4] - 2026-08-31

### 修复

- 凭证过期或本机环境变量中的 AK/SK 无效时，面板不再显示英文通用报错，统一提示前往「设置 → 插件 → CloudQ」检查配置。
- 用量 / 灵感 / 制品 / 架构图视图在请求数据前先检查凭证状态，未配置时直接展示配置引导。

## [0.1.3] - 2026-08-31

### 文档

- README 改为英文，中文版移至 `README-zh.md`，两者互链。
- 新增 `CHANGELOG.md` 与 `DEVELOPMENT.md`（开发、调试、发版与社区收录维护指南）。

## [0.1.2] - 2026-08-31

### 调整

- 插件管理列表移除「本插件」标记，界面更简洁。

## [0.1.1] - 2026-08-28

### 修复

- 修复 Cordis `ctx.effect` 误用导致的启动崩溃（`Invalid effect`），0.1.0 无法启动，请直接升级。

### 调整

- 凭证方式简化为仅保留腾讯云 AK/SK 配置，移除 OAuth 授权链接与授权码流程及对应接口。
- 设置卡片移除「未登录」状态展示；测试连接 / 保存配置成功后显示「AKSK有效」，密钥无效时提示重新配置。
- 保存配置前先执行测试连接，验证通过才写入凭证。
- 无凭证时，用量 / 灵感 / 制品 / 架构图等面板统一提示前往「设置 → 插件 → CloudQ」配置。
- 用量视图改为「累计消耗 Credit」单卡片（含「公测期免费」说明提示），明细表新增最近 3 天 / 1 周 / 1 月 / 自定义日期的时间筛选，积分数值千分位展示。

### 安全

- 修复用量卡片远程数据经 `innerHTML` 渲染的 DOM XSS 风险，改为文本节点渲染。
- 移除客户端暴露到 `window` 的调试对象。

## [0.1.0] - 2026-08-28

### 新增

- 首个公开发布：CloudQ 对话模式（输入栏入口 + `/cloudq` Skill）。
- 能力面板：用量、灵感、制品、架构图四个视图。
- 设置卡片：AK/SK 凭证配置与连接测试。
- 插件管理：Profile 静态插件的可视化热启停。
